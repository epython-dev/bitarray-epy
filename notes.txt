Notes:

  * Currently running 176 (of the 410) tests in 6.7s
    (running these tests with regular bitarray takes 0.42s)

  * Using bytearray object as buffer

  * Still missing:
     - bitwise operations
     - .search()
     - .itersearch()
     - .fromfile()
     - .tofile()
     - .encode()
     - .decode()
     - .iterdecode()
     - decodetree
     - bitarray.util

  * Not sure how to implement memoryview(a)

  * How can standard library calls (memmove(), memcpy(), etc.) be made?
